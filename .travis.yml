sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t paula/docker-react -f dockerfile.dev .

script:
  - docker run -e CI=true paula/docker-react npm run test